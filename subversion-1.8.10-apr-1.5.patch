--- subversion-1.8.10/subversion/libsvn_subr/io.c.0001~	2014-03-28 05:00:59.000000000 +0100
+++ subversion-1.8.10/subversion/libsvn_subr/io.c	2014-10-21 13:36:02.478115965 +0200
@@ -305,7 +305,7 @@ io_check_path(const char *path,
     *kind = svn_node_none;
   else if (SVN__APR_STATUS_IS_ENOTDIR(apr_err))
     *kind = svn_node_none;
-  else if (apr_err)
+  else if (apr_err && apr_err != APR_INCOMPLETE)
     return svn_error_wrap_apr(apr_err, _("Can't check path '%s'"),
                               svn_dirent_local_style(path, pool));
   else
@@ -1530,7 +1530,7 @@ io_set_file_perms(const char *path,
     {
       if (ignore_enoent && APR_STATUS_IS_ENOENT(status))
         return SVN_NO_ERROR;
-      else if (status != APR_ENOTIMPL)
+      else if (status != APR_ENOTIMPL && status != APR_INCOMPLETE)
         return svn_error_wrap_apr(status,
                                   _("Can't change perms of file '%s'"),
                                   svn_dirent_local_style(path, pool));
@@ -1612,7 +1612,7 @@ io_set_file_perms(const char *path,
 
   if (ignore_enoent && APR_STATUS_IS_ENOENT(status))
     return SVN_NO_ERROR;
-  else if (status == APR_ENOTIMPL)
+  else if (status == APR_ENOTIMPL || status == APR_INCOMPLETE)
     {
       /* At least try to set the attributes. */
       apr_fileattrs_t attrs = 0;
@@ -3612,7 +3612,7 @@ svn_io_stat(apr_finfo_t *finfo, const ch
   wanted &= ~SVN__APR_FINFO_MASK_OUT;
 
   status = apr_stat(finfo, fname_apr, wanted, pool);
-  if (status)
+  if (status && status != APR_INCOMPLETE)
     return svn_error_wrap_apr(status, _("Can't stat '%s'"),
                               svn_dirent_local_style(fname, pool));
 
--- subversion-1.8.10/subversion/libsvn_wc/workqueue.c.0001~	2013-07-09 06:00:38.000000000 +0200
+++ subversion-1.8.10/subversion/libsvn_wc/workqueue.c	2014-10-21 11:37:04.567352130 +0200
@@ -1571,7 +1571,7 @@ svn_wc__wq_run(svn_wc__db_t *db,
 
       err = dispatch_work_item(&wib, db, wri_abspath, work_item,
                                cancel_func, cancel_baton, iterpool);
-      if (err)
+      if (err && err != APR_INCOMPLETE)
         {
           const char *skel = svn_skel__unparse(work_item, scratch_pool)->data;
 
